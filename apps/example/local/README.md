# Example App Templates

This folder stores reusable example configuration files.

Use these as templates and copy them into your real app profile path, for example:
- from apps/example/local/roles.example.json
- to apps/BookingPlatform/local/roles.json

Do not run tests directly against this example app folder.
